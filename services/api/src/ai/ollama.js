const U=process.env.OLLAMA_URL||"http://127.0.0.1:11434";export async function ollamaChat(model,messages){const p=messages.map(m=>`${m.role}: ${m.content}`).join("\n")+"\nassistant:";const r=await fetch(`${U}/api/generate`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({model,prompt:p,stream:false,options:{temperature:0.7}})});const d=await r.json();return d.response||"No response.";}
export async function* ollamaChatStream(model,messages){const p=messages.map(m=>`${m.role}: ${m.content}`).join("\n")+"\nassistant:";const r=await fetch(`${U}/api/generate`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({model,prompt:p,stream:true,options:{temperature:0.7}})});const rd=r.body.getReader();const dc=new TextDecoder();let b="";while(true){const{done,value}=await rd.read();if(done)break;b+=dc.decode(value,{stream:true});const ls=b.split("\n");b=ls.pop()||"";for(const l of ls){if(!l.trim())continue;try{const p=JSON.parse(l);if(p.response)yield p.response;}catch{}}}}
