import"dotenv/config";import{pool}from"./db.js";async function m(){await pool.query("CREATE TABLE IF NOT EXISTS users(id TEXT PRIMARY KEY,email TEXT UNIQUE NOT NULL,password_hash TEXT,google_id TEXT UNIQUE,name TEXT,tier TEXT DEFAULT 'free',questions_remaining INTEGER DEFAULT 5,lockout_until TIMESTAMPTZ,stripe_customer_id TEXT,stripe_subscription_id TEXT,created_at TIMESTAMPTZ DEFAULT NOW(),updated_at TIMESTAMPTZ DEFAULT NOW())");await pool.query("CREATE TABLE IF NOT EXISTS chats(id TEXT PRIMARY KEY,user_id TEXT REFERENCES users(id)ON DELETE CASCADE,title TEXT,messages JSONB DEFAULT '[]',created_at TIMESTAMPTZ DEFAULT NOW(),updated_at TIMESTAMPTZ DEFAULT NOW())");await pool.query("CREATE TABLE IF NOT EXISTS api_keys(id TEXT PRIMARY KEY,user_id TEXT REFERENCES users(id)ON DELETE CASCADE,key_hash TEXT NOT NULL,name TEXT,created_at TIMESTAMPTZ DEFAULT NOW())");console.log("Migration done.");process.exit(0);}m().catch(e=>{console.error("Migration failed:",e);process.exit(1);});
